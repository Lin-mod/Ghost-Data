# Ghost-Data
An Upload Labs mod that collects time based game data and stores it in the game files. Those data can be loaded as ghost data to another run. That run can be measured base on 'the delta' to the ghost data.
