# Ghost-Data
A Upload Labs mod that collect time based game data and stores it in the game files. Those data can be loaded as ghost data to a new run, so the new run can measured base on 'the delta' to the ghost data.
